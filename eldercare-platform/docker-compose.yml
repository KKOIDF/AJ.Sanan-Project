services:
  api:
    build: ./services/api
    restart: unless-stopped
    environment:
      OFFLINE_ONLY: "1"
      OFFLINE_OUTPUTS_DIR: /data/outputs
    ports:
      - "8000:8000"
    volumes:
      - ../outputs:/data/outputs:ro
      - ./web:/app/web:ro
