# Eldercare Monitoring System 🏥

Modern web application สำหรับการดูแลผู้สูงอายุ ใช้ Node.js + React + TypeScript

## ✨ คุณสมบัติ

- 🎯 **ง่ายต่อการใช้งาน**: ปุ่มใหญ่ ฟอนต์ชัด เหมาะสำหรับผู้สูงอายุ
- 📊 **วิเคราะห์ความเสี่ยง**: แสดงระดับความเสี่ยงแบบ Real-time
- 🔍 **ค้นหาข้อมูล**: ค้นหาผู้ป่วยได้อย่างรวดเร็ว
- 📱 **Responsive Design**: ใช้งานได้ทั้งมือถือและคอมพิวเตอร์
- 🌐 **เซิร์ฟเวอร์ไทย**: ภาษาไทยทั้งหมด
- ⚡ **Performance**: โหลดเร็ว ทำงานลื่น

## 🚀 วิธีเริ่มใช้งาน

### 1. ติดตั้ง Dependencies

```bash
cd eldercare-web
npm install
```

### 2. เริ่มเซิร์ฟเวอร์

```bash
npm start
```

### 3. เปิดเว็บไซต์

เปิด: http://localhost:3001

## 📁 โครงสร้างไฟล์

```
eldercare-web/
├── server.js          # Node.js Express Server
├── package.json       # Dependencies และ Scripts
├── public/
│   └── index.html     # React App (CDN Version)
└── client/            # React TypeScript App (สำหรับ Production)
```

## 🛠 เทคโนโลジีที่ใช้

### Backend (Node.js)
- **Express.js** - Web Framework
- **CORS** - Cross-Origin Resource Sharing
- **Helmet** - Security Headers
- **csv-parser** - อ่านไฟล์ CSV
- **Morgan** - HTTP Request Logger

### Frontend (React)
- **React 18** - UI Library
- **Tailwind CSS** - Styling Framework
- **TypeScript** - Type Safety
- **Responsive Design** - Mobile First

## 📊 API Endpoints

| Method | Endpoint | คำอธิบาย |
|--------|----------|----------|
| GET | `/api/health` | ตรวจสอบสถานะระบบ |
| GET | `/api/subjects` | ข้อมูลผู้ป่วยทั้งหมด |
| GET | `/api/risk-levels` | ระดับความเสี่ยง (Low/Medium/High) |
| GET | `/api/device-quality` | คุณภาพข้อมูลเซ็นเซอร์ |
| GET | `/api/users/:id/dashboard` | แดชบอร์ดผู้ใช้ |
| POST | `/api/auth/login` | เข้าสู่ระบบ |

### ตัวอย่างการใช้ API

```javascript
// ดึงข้อมูลผู้ป่วย
fetch('/api/subjects?limit=50')
  .then(res => res.json())
  .then(data => console.log(data));

// ดึงระดับความเสี่ยง
fetch('/api/risk-levels?method=quantile')
  .then(res => res.json())
  .then(data => console.log(data));
```

## 🎨 UI/UX Features

- **ปุ่มฉุกเฉิน**: สีแดงกะพริบ พร้อมเอฟเฟกต์
- **การแจ้งเตือน**: แสดงข้อความพร้อมเวลา หายไปอัตโนมัติ
- **ตารางข้อมูล**: เรียงลำดับได้ มี Hover Effect
- **Loading States**: แสดง Spinner ขณะโหลดข้อมูล
- **Color Coding**: 
  - 🟢 ความเสี่ยงต่ำ (เขียว)
  - 🟡 ความเสี่ยงปานกลาง (ส้ม)  
  - 🔴 ความเสี่ยงสูง (แดง)

## 🔧 การ Customize

### เปลี่ยนสีธีม
แก้ไขใน `public/index.html` ส่วน CSS:

```css
.gradient-bg {
    background: linear-gradient(135deg, #your-color-1, #your-color-2);
}
```

### เพิ่ม API Endpoint
แก้ไขในไฟล์ `server.js`:

```javascript
app.get('/api/your-endpoint', (req, res) => {
    res.json({ message: 'Your data' });
});
```

## 🔒 Security Features

- **Helmet.js**: ป้องกัน XSS, Clickjacking
- **CORS**: จำกัดการเข้าถึงจาก Domain อื่น
- **Input Validation**: ตรวจสอบข้อมูลที่ส่งเข้ามา
- **Error Handling**: จัดการ Error อย่างปลอดภัย

## 📈 Performance

- **Caching**: เก็บข้อมูล CSV ใน Memory
- **Lazy Loading**: โหลดข้อมูลเมื่อต้องการ
- **Minification**: บีบอัดไฟล์ JavaScript/CSS
- **CDN**: ใช้ React CDN สำหรับ Development

## 🛠 การ Deploy

### Development
```bash
npm start
```

### Production
```bash
npm run build
npm start
```

## 🐛 Troubleshooting

### ปัญหา: ไม่พบไฟล์ CSV
**แก้ไข**: ตรวจสอบว่าไฟล์ `merged_scored.csv` และ `qc_sensor_counts.csv` อยู่ในโฟลเดอร์ `../outputs`

### ปัญหา: API ไม่ทำงาน
**แก้ไข**: ตรวจสอบ Console ใน Browser และ Terminal

### ปัญหา: หน้าเว็บโหลดไม่ขึ้น
**แก้ไข**: ตรวจสอบ Port 3001 ว่าถูกใช้งานแล้วหรือไม่

## 📞 การติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา

---

**สร้างด้วย ❤️ สำหรับการดูแลผู้สูงอายุที่ดีกว่า**